# API Layer (R3)

Depends on: Domain (R1), Database (R2)

## Examples
- REST endpoints
- GraphQL resolvers
- Request handlers
- Response formatting

## Rules
Can import from: R1, R2
Cannot import from: R4
